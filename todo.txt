This is taking a lot of time
I have an idea to make it shorter
I want to gather all data about the project and user seperatly using the global endpoint
Then parse the data with code rather than request
Tell me if this would work Can you first fetch all the users of the new common core
Then parse throuth thoses users to get only thoses that are from the havre
Then get the data of all users for each python modules
And then use the 2 data sets to find the data needed
also cache as much data as possible to avoid re asking the api for data already asked
also i only want the parse the python modules from the new common core